import { Connection, PublicKey, SendOptions, Signer, TransactionSignature } from "@solana/web3.js";
/** Cancel a config transaction on behalf of the `member`. */
export declare function proposalCancel({ connection, feePayer, member, multisigPda, transactionIndex, memo, sendOptions, }: {
    connection: Connection;
    feePayer: Signer;
    member: Signer;
    multisigPda: PublicKey;
    transactionIndex: bigint;
    memo?: string;
    sendOptions?: SendOptions;
}): Promise<TransactionSignature>;
