import { Connection, PublicKey, SendOptions, Signer } from "@solana/web3.js";
export declare function multisigAddVault({ connection, feePayer, multisigPda, configAuthority, vaultIndex, memo, signers, sendOptions, }: {
    connection: Connection;
    feePayer: Signer;
    multisigPda: PublicKey;
    configAuthority: PublicKey;
    vaultIndex: number;
    memo?: string;
    signers?: Signer[];
    sendOptions?: SendOptions;
}): Promise<string>;
