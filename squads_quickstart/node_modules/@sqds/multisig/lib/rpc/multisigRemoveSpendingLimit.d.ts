import { Connection, PublicKey, SendOptions, Signer, TransactionSignature } from "@solana/web3.js";
/**
 * Create a new spending limit for the controlled multisig.
 */
export declare function multisigRemoveSpendingLimit({ connection, feePayer, multisigPda, configAuthority, spendingLimit, rentCollector, memo, signers, sendOptions, }: {
    connection: Connection;
    feePayer: Signer;
    multisigPda: PublicKey;
    spendingLimit: PublicKey;
    configAuthority: PublicKey;
    rentCollector: PublicKey;
    memo?: string;
    signers?: Signer[];
    sendOptions?: SendOptions;
}): Promise<TransactionSignature>;
