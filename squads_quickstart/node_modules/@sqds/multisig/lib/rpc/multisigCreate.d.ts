import { Connection, PublicKey, SendOptions, Signer, TransactionSignature } from "@solana/web3.js";
import { Member } from "../generated";
/** Creates a new multisig. */
export declare function multisigCreate({ connection, createKey, creator, multisigPda, configAuthority, threshold, members, timeLock, memo, sendOptions, }: {
    connection: Connection;
    createKey: PublicKey;
    creator: Signer;
    multisigPda: PublicKey;
    configAuthority: PublicKey | null;
    threshold: number;
    members: Member[];
    timeLock: number;
    memo?: string;
    sendOptions?: SendOptions;
}): Promise<TransactionSignature>;
