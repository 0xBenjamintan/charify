import { PublicKey, VersionedTransaction } from "@solana/web3.js";
export declare function multisigAddVault({ blockhash, feePayer, multisigPda, configAuthority, vaultIndex, memo, }: {
    blockhash: string;
    feePayer: PublicKey;
    multisigPda: PublicKey;
    configAuthority: PublicKey;
    vaultIndex: number;
    memo?: string;
}): VersionedTransaction;
