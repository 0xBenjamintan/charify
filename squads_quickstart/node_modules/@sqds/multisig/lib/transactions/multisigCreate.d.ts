import { PublicKey, VersionedTransaction } from "@solana/web3.js";
import { Member } from "../generated";
/** Returns unsigned `VersionedTransaction` that needs to be signed by `creator` and `createKey` before sending it. */
export declare function multisigCreate({ blockhash, configAuthority, createKey, creator, multisigPda, threshold, members, timeLock, memo, }: {
    blockhash: string;
    createKey: PublicKey;
    creator: PublicKey;
    multisigPda: PublicKey;
    configAuthority: PublicKey | null;
    threshold: number;
    members: Member[];
    timeLock: number;
    memo?: string;
}): VersionedTransaction;
