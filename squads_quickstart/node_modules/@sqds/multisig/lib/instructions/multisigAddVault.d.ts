import { PublicKey } from "@solana/web3.js";
export declare function multisigAddVault({ multisigPda, configAuthority, vaultIndex, memo, }: {
    multisigPda: PublicKey;
    configAuthority: PublicKey;
    vaultIndex: number;
    memo?: string;
}): any;
