import { createMultisigSetTimeLockInstruction } from "../generated";
export function multisigSetTimeLock({ multisigPda, configAuthority, timeLock, memo, }) {
    return createMultisigSetTimeLockInstruction({
        multisig: multisigPda,
        configAuthority,
    }, {
        args: {
            timeLock,
            memo: memo ?? null,
        },
    });
}
