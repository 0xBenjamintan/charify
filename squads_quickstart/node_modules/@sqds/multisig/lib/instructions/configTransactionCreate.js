import { createConfigTransactionCreateInstruction, } from "../generated";
import { getTransactionPda } from "../pda";
export function configTransactionCreate({ multisigPda, transactionIndex, creator, rentPayer, actions, memo, }) {
    const [transactionPda] = getTransactionPda({
        multisigPda,
        index: transactionIndex,
    });
    return createConfigTransactionCreateInstruction({
        multisig: multisigPda,
        transaction: transactionPda,
        creator,
        rentPayer: rentPayer ?? creator,
    }, { args: { actions, memo: memo ?? null } });
}
