import { getProposalPda } from "../pda";
import { createProposalRejectInstruction } from "../generated";
export function proposalReject({ multisigPda, transactionIndex, member, memo, }) {
    const [proposalPda] = getProposalPda({
        multisigPda,
        transactionIndex,
    });
    return createProposalRejectInstruction({ multisig: multisigPda, proposal: proposalPda, member }, { args: { memo: memo ?? null } });
}
