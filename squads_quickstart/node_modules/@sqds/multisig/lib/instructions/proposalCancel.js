import { getProposalPda } from "../pda";
import { createProposalCancelInstruction } from "../generated";
export function proposalCancel({ multisigPda, transactionIndex, member, memo, }) {
    const [proposalPda] = getProposalPda({
        multisigPda,
        transactionIndex,
    });
    return createProposalCancelInstruction({ multisig: multisigPda, proposal: proposalPda, member }, { args: { memo: memo ?? null } });
}
