import { getProposalPda } from "../pda";
import { createProposalApproveInstruction } from "../generated";
export function proposalApprove({ multisigPda, transactionIndex, member, memo, }) {
    const [proposalPda] = getProposalPda({
        multisigPda,
        transactionIndex,
    });
    return createProposalApproveInstruction({ multisig: multisigPda, proposal: proposalPda, member }, { args: { memo: memo ?? null } });
}
