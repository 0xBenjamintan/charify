import { PublicKey } from "@solana/web3.js";
export declare function proposalCreate({ multisigPda, creator, rentPayer, transactionIndex, isDraft, }: {
    multisigPda: PublicKey;
    /** Member of the multisig that is creating the proposal. */
    creator: PublicKey;
    /** Payer for the proposal account rent. If not provided, `creator` is used. */
    rentPayer?: PublicKey;
    transactionIndex: bigint;
    isDraft?: boolean;
}): import("@solana/web3.js").TransactionInstruction;
