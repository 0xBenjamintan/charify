import { PublicKey, TransactionInstruction } from "@solana/web3.js";
import { Member } from "../generated";
export declare function multisigCreate({ creator, multisigPda, configAuthority, threshold, members, timeLock, createKey, memo, }: {
    creator: PublicKey;
    multisigPda: PublicKey;
    configAuthority: PublicKey | null;
    threshold: number;
    members: Member[];
    timeLock: number;
    createKey: PublicKey;
    memo?: string;
}): TransactionInstruction;
