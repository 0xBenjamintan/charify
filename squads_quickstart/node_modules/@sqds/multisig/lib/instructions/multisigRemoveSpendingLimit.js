import { createMultisigRemoveSpendingLimitInstruction, } from "../generated";
export function multisigRemoveSpendingLimit({ multisigPda, configAuthority, spendingLimit, rentCollector, memo, }) {
    return createMultisigRemoveSpendingLimitInstruction({
        multisig: multisigPda,
        spendingLimit: spendingLimit,
        configAuthority: configAuthority,
        rentCollector: rentCollector,
    }, {
        args: {
            memo: memo ?? null,
        },
    });
}
