import { PublicKey } from "@solana/web3.js";
export declare function batchCreate({ multisigPda, creator, rentPayer, batchIndex, vaultIndex, memo, }: {
    multisigPda: PublicKey;
    /** Member of the multisig that is creating the batch. */
    creator: PublicKey;
    /** Payer for the batch account rent. If not provided, `creator` is used. */
    rentPayer?: PublicKey;
    batchIndex: bigint;
    vaultIndex: number;
    memo?: string;
}): import("@solana/web3.js").TransactionInstruction;
