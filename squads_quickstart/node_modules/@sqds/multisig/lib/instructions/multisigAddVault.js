import { createMultisigAddVaultInstruction } from "../generated";
export function multisigAddVault({ multisigPda, configAuthority, vaultIndex, memo, }) {
    return createMultisigAddVaultInstruction({
        multisig: multisigPda,
        configAuthority,
    }, {
        args: {
            vaultIndex,
            memo: memo ?? null,
        },
    });
}
